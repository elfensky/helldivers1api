services:
    helldiversbot:
        container_name: helldivers1api
        restart: unless-stopped
        image: ghcr.io/elfensky/helldivers1api:dev
        ports:
            - '58102:3000'
        env_file:
            - .env.docker
